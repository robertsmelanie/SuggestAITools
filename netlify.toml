[build]
  publish = "."
  command = ""        # or your build command, if applicable

[functions]
  directory = "api"

[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/:splat"
  status = 200
